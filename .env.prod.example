# Frontend production env
# Fill values for your hosting environment. Do NOT commit real secrets.

# Public API base URL the browser calls.
# In production this should be the *HTTPS* URL of the deployed API.
NEXT_PUBLIC_API_BASE_URL=https://mytegroup-api-estimating-platform-development-691fbc.mytecody.com

# Optional: these are not required by the app runtime if NEXT_PUBLIC_API_BASE_URL is set,
# but keeping them here can help local tooling/scripts.
NEXT_PUBLIC_CLIENT_PORT=443
NEXT_PUBLIC_API_PORT=443
NEXT_PUBLIC_ROOT_DOMAIN_PROD=mytegroup-web-estimating-platform-development-691fbc.mytecody.com
NEXT_PUBLIC_ROOT_DOMAIN_DEV=localhost
